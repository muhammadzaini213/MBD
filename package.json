{
  "name": "discord-webhook-manager",
  "version": "1.0.0",
  "private": true,
  "description": "REST API Express.js + PostgreSQL untuk manajemen Discord Webhook",
  "main": "src/app.js",
  "scripts": {
    "dev": "node --watch src/app.js",
    "start": "node src/app.js",
    "test": "jest --verbose",
    "test:watch": "jest --watchAll",
    "db:migrate": "node scripts/migrate.js"
  },
  "dependencies": {
    "bcrypt": "^5.1.1",
    "dotenv": "^16.4.7",
    "express": "^5.1.0",
    "jsonwebtoken": "^9.0.2",
    "pg": "^8.13.1"
  },
  "devDependencies": {
    "jest": "^30.5.1",
    "supertest": "^7.2.2"
  }
}
